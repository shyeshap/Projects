package singlyLinkedList;

public interface listIterator {
		/** 
		 * @return
		 * Object data of current iterator
		 * @set
		 * set iterator to next node
		 */
		public Object next();
		public Boolean hasNext();
}
